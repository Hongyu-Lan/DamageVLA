# 采集与标定协议（2026-09-16）

适用于 Task 2（纸盒）以及之后要补采的 Task 1（果蔬）。
相关分析见 `data_analysis_20260916.md`，训练侧改动见 `todo_training_contract.md`。

---

## 0. 为什么从纸杯换成直壁纸盒

原设计用带盖纸杯（上宽下窄）。锥形杯在向下滑动时，进入夹爪的是更宽的截面，会**楔紧自锁**，
打滑这个失败模式在几何上被杜绝，$f_{\min}$ 趋近于 0。那样两个条件的安全区间就变成嵌套
（空 $[0, f_{\max}^{空}]$、满 $[0, f_{\max}^{满}]$），一个很轻的恒定力对两者都安全，
**Task 2 退化成 Task 1，力反馈不再必要**。

直壁纸盒没有锥度，打滑只由摩擦决定，$f_{\min}$ 随重量增长，设计才成立。
实测已确认：10 g 空盒与 250 g 满盒的安全区间不重叠——提起满盒所需的最小力已经让空盒明显变形。

---

## 1. 示教采集

### 1.1 核心原则

**示教只负责"该用多大力"，上下界交给标定。** 每次都温柔、稳定地抓起来，判断标准固定：
抓稳且无可见变形就停。**不要刻意去采夹很紧或夹很松的示教**——超出 $f_{\max}$ 的不是安全示教，
低于 $f_{\min}$ 的会打滑，两种都会被过滤掉，采了也是白采，还会污染组统计。

### 1.2 分段闭合（重要）

一把闭到底的示教里，没有任何"感觉到力之后再决定"的动作，模型学不到反馈行为，推理时也只会闭到底。
正确做法：**闭一点 → 保持住 → 感受 → 再闭一点**，直到判断抓稳。

- **中途不要张开。** 阶段标注脚本判定 grasp 的规则是"提起之前最后一次张开之后"，
  中间任何一次回张，都会把前面所有闭合帧判成 `prepare`，最有信息的部分全丢。
  需要松开重试的，整条 episode 作废重来。
- **分几段必须由力决定，不能是固定套路**（比如满盒永远按 5 下）。两种条件的接近方式和第一段增量必须完全一样，
  之后的分歧只能来自手上的感觉。
- 实测效果：纸盒批次 grasp 阶段占 8.3%，果蔬批次（一把闭到底）只有 3.9%。

### 1.3 操作员对装填量盲测

纸盒不透明、外观一致，可以让另一个人准备好递过来，遥操作的人不知道里面有没有沙子。
这样示教行为**必然**由力驱动，不可能有先验套路。论文里也能写"采集时操作员对装填量是盲的"，
直接挡掉"是不是操作员知道答案所以演出来了"这个质疑。

### 1.4 要变的和不要变的

| 每次都要变 | 必须固定 |
|---|---|
| 物体摆放位置、朝向 | 判断标准（抓稳且无变形就停） |
| 同一条件用多个实例（多个纸盒） | 抓握高度 |
| 采集时段内两种条件交替 | 闭合速度与分段方式 |

摆放位置和朝向必须变，否则接触发生时的开度每次都一样，策略可以学成开环的"闭到 X mm 就停"，
根本不看力。位置一变，这条捷径就走不通。

两种条件**在每个 session 内交替采**：一种条件独占一个时段，会让条件和光照、标定漂移、
操作员状态混在一起，外观对照就失效了。

### 1.5 每条 episode 的固定动作

- 开头留 10–20 帧**夹爪张开且静止**的窗口（逐 episode 归零要用，代码里会取 `stage == prepare`
  且 `cmd_speed_l` 全零的前若干帧）。
- 抓稳后**保持 1–2 秒再提**，让 grasp 阶段有足够帧数，力读数也更稳。
- 每个 session 开头做一次**参考挤压**：夹一个固定的参考物到固定开度，记一次读数，用于事后检测增益漂移。
- 传感器配置（增益、采样、指尖垫、安装位置）从第一批到最后一批都不能变。

### 1.6 失败处理与记录

- 中途打滑或明显变形 → 立刻停、标记、换实例重来。这条 episode 不进训练，但**不要删**，留作失败分析。
- 提起后才发现变形的，事后标记并排除。
- **每条 episode 结束后当场记录**：结果（安全成功 / 变形 / 打滑 / 其他失败）、实例编号、条件、
  沙子质量。事后补不回来。
- 压坏的纸盒立即退役——塑性变形会让它的 $f_{\max}$ 下降，再用会污染数据。

---

## 2. 标定

### 2.1 目的与分工

标定测的是**边界**：$f_{\min}$（打滑下界）和 $f_{\max}$（变形上界）。它不进训练，只用于

1. 验证示教落在区间内；
2. 计算违规率 $v^{+}, v^{-}$；
3. 证明 Task 2 两个条件的区间不重叠；
4. 过滤抓过头的示教（见 `todo_training_contract.md` 的排除规则）。

### 2.2 流程

对每个条件，脚本闭合到指定开度，走完整流程（闭合 → 提起 → 平移 → 放下 → 松开），每次判为
slip / success / deformation：

- **空盒测上界**：逐步夹更紧，直到永久变形。破坏性，每个判为"太紧"的试验消耗一个盒子。
- **满盒测下界**：从松到紧，直到提起平移都不打滑。非破坏性，同一个盒子可以多试，但反复夹会累积微损伤。
- **打滑必须提起之后才能判定**，不能只闭合不提；滑落发生后该试验即可结束，但要记下当时的开度和力。
- **变形要放下松开后检查**，所以这类试验要跑完。
- 每个条件每个边界**至少 3 个实例**，取最保守的一对（最小的 $f_{\max}$、最大的 $f_{\min}$）。
  单实例的点估计只够做设计验证，不够写论文。
- **半盒（中间装填量）的两个边界也要测**，泛化分析和违规率要用。

### 2.3 与示教保持一致

- **抓握高度**必须和示教一致，否则边界没有可比性。
- **闭合方式**要一致：示教是分段闭合加保持，标定若是一次闭到指定开度，同一开度下的读数会因应力松弛而不同。
  要么标定也分段，要么至少在最终开度上保持同样时长再读数。

### 2.4 边界数值的读法（提前定死）

- $f_{\max}$：没有造成变形的试验中，抓取阶段观测到的**最大稳态**抓握力；
- $f_{\min}$：完成提起和平移且没打滑的试验中，观测到的**最小稳态**抓握力；
- 一律用稳态值（闭合到位保持那一段的均值），不取接触瞬间的尖峰。

### 2.5 每条标定试验必须记录的元数据

传感器流已经全有，缺的全是元数据，而且事后补不回来。建议每条写一个 sidecar JSON：

```json
{"corpus": "calibration", "condition": "box_empty|box_filled|box_half",
 "instance_id": "...", "fill_mass_g": 250, "commanded_aperture_m": 0.028,
 "outcome": "slip|success|deformation", "outcome_stage": "grasp|lift|translate|place",
 "outer_diameter_before_mm": 0.0, "outer_diameter_after_mm": 0.0,
 "grip_height_mm": 0.0, "note": ""}
```

### 2.6 电压到牛顿的换算（可选，训练不依赖）

共享尺度的仿射变换不改变高斯 KL，所以**训练可以直接用电压单位**，换算事后补或不补。
需要牛顿时：

- **滑落起始法**：夹住已知质量、缓慢张开到刚开始滑，$2\mu N = mg$。换 5–6 个质量各做 3 次，
  得到 taxel 总和与 $mg$ 的直线。斜率里混着灵敏度 $k$ 和摩擦系数 $\mu$，单独用它只能标到
  "等效可托质量"这个单位。
- **斜面法测 $\mu$**：把盒子材料放在指尖垫材料的斜面上，抬到刚滑动，$\mu = \tan\theta$，做 3–5 次。
- **电子秤法**：指尖隔着一块盒子材料压电子秤，取 5–6 个开度点，直接得到 $S \leftrightarrow N$。
- **交叉验证**：秤法给 $k$，滑落法给斜率 $a = k/(2\mu)$，两者相除得 $\mu$，和斜面法对比。接近说明整条链路可信。

**唯一必须在建标签前确认的是线性度**：若滑落曲线明显非线性（触觉阵列大力端常见饱和），
必须先把拟合出的单调映射套到标签上，否则高力端的 $\sigma$ 被压缩，分布形状和真实力空间不一致。

---

## 3. 数量

| 项目 | 数量 |
|---|---|
| Task 2 示教 | 空盒、满盒各 40 条以上（含验证集留出） |
| Task 2 标定 | 3 条件（空/满/半）× 3–5 实例 × 约 7 次二分 |
| Task 1 补采（若做） | 用本协议重采，重点是停位一致 |
| 评估用纸盒备货 | 60–80 个，同批次同规格（不同批次壁厚不同，$f_{\max}$ 会漂） |
