# Four-A100 end-to-end gate. It verifies JAX's multi-GPU mesh and collectives
# before the long run while preserving the full run's per-GPU batch of four.
source train_config_leonardo_smoke.conf

BATCH_SIZE=16
FSDP_DEVICES=1
EXP_NAME=draftvla_leonardo_smoke4
